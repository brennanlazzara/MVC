<h1>Burgers!</h1>

<h2>Burgers that do not taste good!</h2>

<ul>
  {{#each burgers}}
    {{#unless tastes_Good}}
      {{> burgers/burger-block tastes_Good=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that Taste Good!</h2>

<ul>
  {{#each burgers}}
    {{#if tastes_Good}}
      {{> burgers/burger-block new_tastes_Good=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="tastes_Good">Tastes Good?</label><br>
    <input type="radio" name="tastes_Good" value="1" checked> Tastes Good!<br>
    <input type="radio" name="tastes_Good" value="0"> Not Delicious
  </div>

  <button type="submit">Add Burger</button>
</form>
